Here is configuration management:

tasks are:

#0.Using Puppet, create a file in /tmp.
#1.Using Puppet, install flask from pip3
#2.Using Puppet, create a manifest that kills a process named killmenow.
